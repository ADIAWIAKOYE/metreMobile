import 'package:flutter/material.dart';

class AddMesurePage extends StatelessWidget {
  const AddMesurePage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("Add mesures page"),
        ),
      ),
    );
  }
}
