import 'package:flutter/material.dart';

class MesurePage extends StatelessWidget {
  const MesurePage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("Mesures page"),
        ),
      ),
    );
  }
}
